---
- name: Criar symlinks para dotfiles
  ansible.builtin.file:
    src: "{{ workstation_repo }}/dotfiles/{{ item.src }}"
    dest: "{{ ansible_facts.env.HOME }}/{{ item.dest }}"
    state: link
    force: true
    follow: false
  loop: "{{ user_dotfiles_dotfiles }}"
