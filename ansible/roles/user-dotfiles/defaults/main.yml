---
# defaults file for dotfiles role
dotfiles:
  - src: bash/.bashrc
    dest: .bashrc
  - src: bash/.profile
    dest: .profile
  - src: bash/.bash_aliases
    dest: .bash_aliases
  - src: git/.gitconfig
    dest: .gitconfig